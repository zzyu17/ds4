Speculative decoding preserves exact greedy output by using a **verification step** that ensures the final sequence is identical to what would have been generated by the target model alone, token-by-token. Here’s how it works:

### Core Mechanism
1. **Draft Phase**: A smaller, faster "draft" model generates