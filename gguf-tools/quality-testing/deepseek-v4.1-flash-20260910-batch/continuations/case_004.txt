A TCP server must preserve a partially written reply when a client reads slowly because TCP send buffers are finite and the application cannot assume that a single `write` or `send` call will deliver the entire response at once. If the client’s receive window fills up, the server’s socket buffer may accept only part